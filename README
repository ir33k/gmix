LIBS AND PROGRAMS FOR GEMINI PROTOCOL

Written in c89 for fun in NOT spare time instead of doing actual work
AKA procrastination via programming.  Please, send help X__x

Build and test all programs with script:

	$ ./build	# Dependencies: stdlib and OpenSSL

Programs:

	$ ./gmiu	# URL parser
	$ ./gmif	# Fetch URL
	$ ./gmip	# Parse text/gemini                     /)_/)
	$ ./gmi2html	# Parse text/gemini to HTML            (-.- )
	$ ./gmic	# Client (interactive TUI)   WIP     (")(")_o

Example of using programs together by passing parsed URL to fetch
program and piping result through parser.

	$ ./gmif $(./gmiu -f tilde.pink)   | ./gmip
	$ ./gmif $(./gmiu -f rawtext.club) | ./gmi2html

TODO Try one of simpler compilers: https://suckless.org/rocks/
